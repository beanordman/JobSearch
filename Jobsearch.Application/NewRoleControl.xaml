<UserControl x:Class="Jobsearch.Application.NewRoleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Loaded="UserControl_Loaded_1">
    <Grid DataContext="{Binding}">
        <ComboBox
            HorizontalAlignment="Left" Margin="10,36,0,0" VerticalAlignment="Top" Width="280"
            ItemsSource="{Binding Path=Clients}"
            DisplayMemberPath="Name"
            />
        <Label Content="Select Client:" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.057,-0.25"/>

    </Grid>
</UserControl>
